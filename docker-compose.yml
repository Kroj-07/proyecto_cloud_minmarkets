version: "3.8"
services:
  inventario:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
  db:
    image: mysql:8
    env_file:
      - .env
    ports:
      - "3306:3306"